{
  "name": "quickreact-api-proxy",
  "version": "1.0.0",
  "description": "API proxy pour QuickReact - cache la clé Giphy",
  "type": "module",
  "scripts": {
    "vercel-build": "echo 'No build step required'",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@lemonsqueezy/lemonsqueezy.js": "^2.0.0",
    "@vercel/analytics": "^1.1.1"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
