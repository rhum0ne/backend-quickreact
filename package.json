{
  "name": "quickreact-api-proxy",
  "version": "1.0.0",
  "description": "API proxy pour QuickReact - cache la clé Giphy",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
